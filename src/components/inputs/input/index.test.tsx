import React from "react";
import { Input } from ".";
import { render } from "../../../test-utils";
describe("<Input />", () => {
  it("should render without error", () => {
    render(<Input />);
  });
});
